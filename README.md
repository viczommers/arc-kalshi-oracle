# Kalshi Oracle

Install with:

```sh
npm i
```

Copy over the .env.example end edit it

```sh
cp .env.example .env
```

Compile:

```sh
npm run compile
```

Edit the scripts set file and run:

```sh
npm run set
```

---
